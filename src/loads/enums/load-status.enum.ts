export enum LoadStatus {
  DRAFT = 'draft',
  ACCEPTED = 'accepted',
  EXPIRED = 'expired',
  CANCELLED = 'cancelled',
  PUBLISHED = 'published',
  IN_TRANSIT = 'in_transit',
  DELIVERED = 'delivered',
}
